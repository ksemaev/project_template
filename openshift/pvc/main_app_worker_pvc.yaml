apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: main_app_worker-pvc
spec:
 accessModes:
  - ReadWriteMany
 storageClassName: glusterfs-storage  
 resources:
   requests:
     storage: 5Gi